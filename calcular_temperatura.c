#include <stdio.h>

// Este programa calcula a temperatura e verifica se está calor ou não.
int main() {
  float temperatura = 32.0;
  
    // Verifica se a temperatura é maior que 30 graus Celsius
  if (temperatura > 30.0) {
    printf("Está calor\n");
  }

  return 0;
}